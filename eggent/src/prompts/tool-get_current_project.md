Use this tool when the user asks:
- which project is active now
- where you are currently working
- what folder/path is currently selected

Behavior:
- Report `projectName`/`projectId` and `currentPath` from the tool result.
- If `isGlobal` is true, state that no project is currently selected.
